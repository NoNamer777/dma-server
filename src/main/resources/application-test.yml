# Application properties config for TEST environment

dma:
  allowed-origins: 'localhost'
logging:
  level:
    web: 'debug'
server:
  address: 'localhost'
spring:
  datasource:
    driver-class-name: 'org.h2.Driver'
    url: 'jdbc:h2:mem:test'
  jpa:
    database-platform: 'org.hibernate.dialect.H2Dialect'
    hibernate:
      ddl-auto: 'validate'
    show-sql: false
